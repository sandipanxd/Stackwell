{
  "name": "stackwell-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK app deploying Stackwell to Lambda + API Gateway. Not deployed automatically — see README.md.",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "aws-cdk": "^2.1142.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.270.0",
    "constructs": "^10.8.1",
    "source-map-support": "^0.5.21"
  }
}
